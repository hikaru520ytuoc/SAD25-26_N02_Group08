generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Sprint 0 intentionally has no business models.
// Business tables such as users, roles, topics, outlines, scores, and archives
// will be added from Sprint 1 onward.
